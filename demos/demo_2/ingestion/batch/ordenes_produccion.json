{
  "_doc": "Ingesta batch de ordenes de produccion — CDC (JSON)",
  "name":        "ordenes_produccion",
  "description": "Carga incremental CDC de ordenes desde Landing hacia Bronze",
  "ingest_type": "batch",
  "load_type":   "cdc",
  "enabled":     true,

  "source": {
    "format": "json",
    "path":   "{path.landing}/ordenes_produccion",
    "options": {
      "multiLine": "false"
    }
  },

  "destination_contract": "../../tables/bronze_new/ordenes_produccion_raw.json",

  "metadata": {
    "add_ingested_at":   true,
    "add_ingested_date": true,
    "add_source_file":   true
  }
}
